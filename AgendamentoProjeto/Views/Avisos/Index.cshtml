@model IEnumerable<AgendamentoProjeto.Models.Aviso>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Agendamento ID
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mensagem)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AgendamentoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mensagem)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AvisosId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AvisosId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AvisosId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
